<UserControl x:Class="Flow.Plugin.TraeWorkspace.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <CheckBox x:Name="DiscoverWorkspacesCheckBox" Content="发现Trae工作区" Margin="0,5,0,5"/>
            <CheckBox x:Name="DiscoverMachinesCheckBox" Content="发现远程SSH机器" Margin="0,5,0,5"/>
            <TextBlock Text="自定义工作区" Margin="0,10,0,5" FontWeight="Bold"/>
            <ListBox x:Name="CustomWorkspacesListBox" Height="150" Margin="0,5,0,5"/>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                <Button x:Name="AddWorkspaceButton" Content="添加" Margin="0,0,5,0"/>
                <Button x:Name="RemoveWorkspaceButton" Content="移除"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>